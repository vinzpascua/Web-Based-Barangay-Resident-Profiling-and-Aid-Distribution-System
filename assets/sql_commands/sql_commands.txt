CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50),
    password VARCHAR(255)
);

ALTER TABLE users
MODIFY COLUMN password VARCHAR(255) NOT NULL;
ALTER TABLE users
ADD COLUMN first_name VARCHAR(50) NOT NULL AFTER id;
ALTER TABLE users
ADD COLUMN last_name VARCHAR(50) NOT NULL AFTER first_name;
ALTER TABLE users
ADD COLUMN role VARCHAR(20) NOT NULL AFTER last_name;

ALTER TABLE registered_resi
ADD address VARCHAR(255) AFTER last_name;

ALTER TABLE registered_resi
ADD birthdate DATE AFTER address;
ALTER TABLE registered_resi
ADD voters_registration_no VARCHAR(50) AFTER occupation;
DESCRIBE users;





CREATE TABLE registered_resi (
    id INT AUTO_INCREMENT PRIMARY KEY,
    first_name VARCHAR(100) NOT NULL,
    last_name VARCHAR(100) NOT NULL,
    age INT NOT NULL,
    gender VARCHAR(10) NOT NULL,
    civil_status VARCHAR(20) NOT NULL,
    contact VARCHAR(20),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

ALTER TABLE registered_resi
ADD COLUMN middle_name VARCHAR(50) AFTER first_name,
ADD COLUMN occupation VARCHAR(100) AFTER civil_status;






CREATE TABLE registered_household (
    id INT AUTO_INCREMENT PRIMARY KEY,
    household_number VARCHAR(50) NOT NULL,
    head_of_family VARCHAR(100),
    address VARCHAR(255) NOT NULL,
    household_members TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

ALTER TABLE registered_household
ADD UNIQUE (household_number);





CREATE TABLE aid_program (
    id INT AUTO_INCREMENT PRIMARY KEY,
    program_name VARCHAR(255),
    aid_type VARCHAR(100),
    date_scheduled DATE,
    beneficiaries INT,
    status VARCHAR(50)
);